// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("dataserv", "for dataserv support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("dataserv", "enable dataserv support", "no");

if (PHP_DATASERV != "no") {
	EXTENSION("dataserv", "dataserv.c");
}

